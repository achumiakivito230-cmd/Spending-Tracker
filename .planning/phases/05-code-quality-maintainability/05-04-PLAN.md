---
phase: 05-code-quality-maintainability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [QUAL-01, QUAL-03]
gap_closure: true

must_haves:
  truths:
    - "DataModule.loadLabels() is called at all load call sites — DataModule._labels is populated at runtime"
    - "DataModule.loadHistory() is called at all load call sites — DataModule._allTxData is populated at runtime"
    - "All labels mutations (handleLabelChange, deleteLabel, renderManageLabels delete handler, addLabel, btn-ml-add) go through DataModule methods"
    - "All allTxData mutations (handleExpenseChange, _doLoadHistory, deleteExpense, saveEdit) go through DataModule methods"
    - "validateLabelExists() in saveEdit() correctly returns true for existing labels and false for deleted ones"
    - "ChartModule data-changed listener fires when mutations occur (DataModule._emit is called on every mutation)"
  artifacts:
    - path: "index.html"
      provides: "All standalone load/mutation code replaced with DataModule method calls"
      must_not_contain:
        - "loadLabels() standalone definition (after LABELS + BUDGET comment, outside DataModule)"
        - "loadHistory() standalone definition (after HISTORY comment, outside DataModule)"
        - "loadMonthTotals() standalone definition (standalone, outside DataModule)"
        - "allTxData = allTxData.filter outside DataModule closure"
        - "labels = labels.filter outside DataModule closure"
        - "labels.unshift outside DataModule closure"
  key_links:
    - from: "onSignedIn / unlockToNumpad Promise.all"
      to: "DataModule.loadLabels() / DataModule.loadMonthTotals()"
      via: "direct method calls replacing standalone function calls"
      pattern: "Promise.all([DataModule.loadLabels(), DataModule.loadMonthTotals()])"
    - from: "handleExpenseChange / handleLabelChange"
      to: "DataModule mutation methods"
      via: "DataModule.setTransactions / DataModule._emit"
      pattern: "DataModule.setTransactions([...]) or inline _allTxData sync + DataModule._emit('data-changed')"
    - from: "validateLabelExists(editLabel.id)"
      to: "DataModule._labels (populated by DataModule.loadLabels)"
      via: "runtime check in saveEdit()"
      pattern: "DataModule._labels.some(l => l.id === labelId) returns correct boolean"
---

<objective>
Wire all load and mutation call sites to use DataModule methods, completing the QUAL-01 refactoring that was skipped in Plan 05-01. Once DataModule.loadLabels() is called at runtime, DataModule._labels is populated and validateLabelExists() becomes functional (QUAL-03).

Purpose: The DataModule factory was created with a complete API but call-site refactoring was deferred. The result is a dual-state situation: DataModule exists but is never called, so _allTxData and _labels are always empty. This makes data-changed events unfireable and label validation broken. This plan completes the wiring.

Output: All standalone loadLabels/loadHistory/loadMonthTotals definitions removed. All ~14 call sites pointing to DataModule methods. All 11 direct allTxData/labels mutations replaced with DataModule method calls. QUAL-01 and QUAL-03 fully satisfied.
</objective>

<execution_context>
@C:/Users/achum/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/achum/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-code-quality-maintainability/05-CONTEXT.md
@.planning/phases/05-code-quality-maintainability/05-VERIFICATION.md
@.planning/phases/05-code-quality-maintainability/05-01-SUMMARY.md

<interfaces>
<!-- DataModule public API — defined at line 1267 of index.html, returned at line 1493 -->
<!-- All methods below exist and are callable as DataModule.methodName() -->

DataModule.getLabels()               — returns [..._labels] (copy)
DataModule.getTransactions()         — returns [..._allTxData] (copy)
DataModule.setLabels(newLabels)      — sets _labels, writes localStorage, does NOT emit
DataModule.setTransactions(newTx)    — sets _allTxData, does NOT emit
DataModule.loadLabels()              — fetches from Supabase, sets _labels, emits 'labels-updated'
DataModule.loadHistory()             — fetches from Supabase, sets _allTxData, emits 'data-changed'
DataModule.loadMonthTotals()         — fetches from Supabase, returns monthTotals object
DataModule.addTransaction(txObj)     — inserts to Supabase, unshifts _allTxData, emits 'data-changed'
DataModule.updateTransaction(id, u)  — updates Supabase, mutates _allTxData[idx], emits 'data-changed'
DataModule.deleteTransaction(id)     — deletes from Supabase, filters _allTxData, emits 'data-changed'
DataModule.addLabel(labelObj)        — inserts to Supabase, unshifts _labels, writes localStorage, emits 'labels-updated'
DataModule.updateLabel(id, updates)  — updates Supabase, mutates _labels[idx], writes localStorage, emits 'labels-updated'
DataModule.deleteLabel(id)           — deletes from Supabase, filters _labels, writes localStorage, emits 'labels-updated'
DataModule.validateLabelExists(id)   — returns _labels.some(l => l.id === id) — ONLY works if loadLabels() was called

NOTE: DataModule does NOT have a user_id filter in loadLabels() — it omits .eq('user_id', ...). The standalone
loadLabels at line 2982 also omits user_id. Both are consistent. DataModule.loadHistory() uses .eq('user_id', currentUser.id).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all standalone load function call sites with DataModule methods and remove standalone definitions</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 2975-3110 (standalone loadLabels, _doLoadLabels, loadMonthTotals definitions to be removed)
    - index.html lines 3374-3410 (standalone loadHistory, _doLoadHistory definitions to be removed)
    - index.html lines 1775-1793 (unlockToNumpad — Promise.all call site at line 1782)
    - index.html lines 2190-2210 (onSignedIn — Promise.all call site at line 2199)
    - index.html lines 2340-2380 (switchTab and slideToHistory/slideToChart — loadHistory call sites at lines 2348, 2350, 2365, 2374, 2375)
    - index.html lines 2960-2975 (setupConnectivityWatch — loadHistory call site at line 2968)
    - index.html lines 3020-3030 (realtimeChannel subscribe handler — loadHistory call site at line 3025)
    - index.html lines 3085-3100 (setupVisibilityWatch — loadHistory call site at line 3093)
    - index.html lines 3160-3175 (budget set handler — loadMonthTotals call site at line 3169)
  </read_first>
  <action>
**Step 1: Replace load function call sites (8 locations)**

Replace each call to the standalone `loadLabels()`, `loadHistory()`, `loadMonthTotals()` with the DataModule equivalent. Because DataModule.loadHistory() does NOT update DOM (no tx-list spinner, no renderFilterChips/renderHistory call), the call sites that trigger DOM rendering MUST still do that rendering themselves using `DataModule.getTransactions()` as the data source.

**Line 1782** — unlockToNumpad:
- Old: `Promise.all([loadLabels(), loadMonthTotals()])`
- New: `Promise.all([DataModule.loadLabels(), DataModule.loadMonthTotals()])`

**Line 2199** — onSignedIn:
- Old: `Promise.all([loadLabels(), loadMonthTotals()])`
- New: `Promise.all([DataModule.loadLabels(), DataModule.loadMonthTotals()])`

**Line 2348** — switchTab, history target:
- Old: `if (target === 'history') loadHistory();`
- New: `if (target === 'history') DataModule.loadHistory().then(txData => { renderFilterChips(txData); renderHistory(txData, null); activeFilter = null; document.getElementById('filter-row').innerHTML = ''; document.getElementById('tx-list').innerHTML = '<div style="color:var(--muted);font-size:14px;padding:28px 0;text-align:center">Loading\u2026</div>'; DataModule.loadHistory().then(txData2 => { renderFilterChips(txData2); renderHistory(txData2, null); }); });`

Wait — the existing `_doLoadHistory()` handles its own DOM rendering (sets Loading... spinner, calls renderFilterChips, renderHistory). DataModule.loadHistory() does NOT do DOM rendering. So the cleanest approach is: keep the standalone `_doLoadHistory()` as the DOM-rendering implementation, but make `loadHistory()` delegate to DataModule and sync the global `allTxData` back.

**REVISED APPROACH for Step 1:** Instead of removing standalone functions completely, convert them to thin wrappers that:
1. Call DataModule method
2. Sync global `allTxData` / `labels` from DataModule getters (for all the rendering code that still reads globals)
3. The DataModule method handles Supabase fetch + updates its own _allTxData

For `loadLabels()` at line 2982 — replace body with wrapper:
```javascript
async function loadLabels() {
  await DataModule.loadLabels();
  labels = DataModule.getLabels(); // sync global
  renderChips();
}
```
Remove the standalone `_labelsLoading` variable at line 2980 (DataModule has its own dedup).
Remove `_doLoadLabels()` function body (lines 2988-2999) — no longer needed.

For `loadMonthTotals()` at line 3099 — replace body with wrapper:
```javascript
async function loadMonthTotals() {
  return DataModule.loadMonthTotals();
}
```

For `loadHistory()` at line 3379 — replace body with wrapper that delegates to DataModule AND syncs global allTxData:
The wrapper must handle its own DOM rendering (spinner, renderFilterChips, renderHistory) since DataModule.loadHistory() is data-only.
```javascript
async function loadHistory() {
  if (_historyLoading) return _historyLoading;
  _historyLoading = _doLoadHistory();
  try { await _historyLoading; } finally { _historyLoading = null; }
}
```
Keep `_historyLoading` at line 3377, keep `loadHistory()` wrapper at 3379 — only change `_doLoadHistory()` at line 3385 to sync `allTxData` from DataModule after fetch. Specifically, after the DataModule fetch completes, assign `allTxData = DataModule.getTransactions()` so all downstream rendering still works.

Change `_doLoadHistory()` at line 3385:
- Replace line 3393 `const { data, error } = await db.from('expenses')...` with `const txData = await DataModule.loadHistory();`
- Remove the error/null checks (DataModule handles them internally with logError + toast)
- Remove `allTxData = data` at line 3404
- Add `allTxData = DataModule.getTransactions();` after the DataModule call
- Keep `renderFilterChips(allTxData)` and `renderHistory(allTxData, null)`
- Keep the `targetScreen` guard: `if (screen !== targetScreen) return;`
- Keep the Loading... spinner at line 3388 and `filter-row` clear at 3389

The new `_doLoadHistory()` body:
```javascript
async function _doLoadHistory() {
  const targetScreen = screen;
  const list = document.getElementById('tx-list');
  list.innerHTML = `<div style="color:var(--muted);font-size:14px;padding:28px 0;text-align:center">Loading\u2026</div>`;
  document.getElementById('filter-row').innerHTML = '';
  activeFilter = null;
  if (!db) { list.innerHTML = `<div style="color:var(--muted);font-size:14px;padding:28px 0;text-align:center">No connection.</div>`; return; }
  await DataModule.loadHistory();
  if (screen !== targetScreen) return;
  allTxData = DataModule.getTransactions();
  renderFilterChips(allTxData);
  renderHistory(allTxData, null);
}
```

**Summary of actual changes:**
1. Line 2980: Remove `let _labelsLoading = null;` (DataModule owns this)
2. Lines 2982-2986: Replace `loadLabels()` body with the wrapper above (3 lines)
3. Lines 2988-2999: Remove `_doLoadLabels()` entirely (DataModule.loadLabels does the same)
4. Lines 3099-3107: Replace `loadMonthTotals()` body with `return DataModule.loadMonthTotals();`
5. Lines 3385-3407: Replace `_doLoadHistory()` body with the new version above (13 lines)

No other call sites need changing — they already call `loadHistory()`, `loadLabels()`, `loadMonthTotals()` which now delegate to DataModule.
  </action>
  <verify>
    <automated>grep -n "DataModule.loadLabels\|DataModule.loadHistory\|DataModule.loadMonthTotals\|DataModule.getTransactions\|DataModule.getLabels" "C:/Users/achum/App Creation/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_doLoadLabels" index.html` returns no results (function removed)
    - `grep -n "DataModule.loadLabels()" index.html` returns at least 1 result (wrapper calls it)
    - `grep -n "DataModule.loadHistory()" index.html` returns at least 1 result (_doLoadHistory calls it)
    - `grep -n "DataModule.loadMonthTotals()" index.html` returns at least 1 result (loadMonthTotals wrapper calls it)
    - `grep -n "DataModule.getTransactions()" index.html` returns at least 1 result (allTxData sync in _doLoadHistory)
    - `grep -n "DataModule.getLabels()" index.html` returns at least 1 result (labels sync in loadLabels wrapper)
    - The two `Promise.all` at lines ~1782 and ~2199 now read `Promise.all([DataModule.loadLabels(), DataModule.loadMonthTotals()])`
    - `_labelsLoading = null` standalone variable at line ~2980 is removed (not the one inside DataModule at line 1272)
  </acceptance_criteria>
  <done>
    - loadLabels(), loadMonthTotals(), loadHistory() all delegate to DataModule equivalents
    - _doLoadLabels() removed — DataModule.loadLabels() replaces it
    - DataModule._labels is populated at runtime (next call to loadLabels() fills it via DataModule)
    - DataModule._allTxData is populated at runtime (next call to loadHistory() fills it via DataModule)
    - Global allTxData and labels kept in sync via DataModule.getTransactions() / DataModule.getLabels() after each load
    - All existing rendering code (renderFilterChips, renderHistory, ChartModule.render) continues to work unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all direct allTxData and labels mutations with DataModule method calls</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 3037-3084 (handleExpenseChange and handleLabelChange — 9 direct mutations)
    - index.html lines 3248-3257 (deleteLabel function — labels = labels.filter at line 3252)
    - index.html lines 3311-3322 (renderManageLabels delete button handler — labels = labels.filter at line 3316)
    - index.html lines 3335-3352 (standalone addLabel function — labels.unshift at line 3344)
    - index.html lines 3913-3926 (btn-ml-add handler — labels.unshift at line 3921)
    - index.html lines 3550-3563 (deleteExpense function — allTxData = allTxData.filter at line 3558)
    - index.html lines 3625-3634 (saveEdit after Supabase update — allTxData[idx] = {...} at line 3629)
  </read_first>
  <action>
Replace every direct mutation of `allTxData` and `labels` outside the DataModule closure with DataModule method calls. After each DataModule mutation call, sync the global variable from DataModule so all downstream rendering code continues to work.

**Important:** DataModule mutation methods (addLabel, deleteLabel, etc.) perform the Supabase operation internally. The existing call sites ALREADY perform the Supabase operation themselves, then mutate the global. The pattern to follow:

Option A (where Supabase is called OUTSIDE DataModule, then global is mutated): Remove the Supabase call from the call site, replace with DataModule method which handles both DB + state.

Option B (where call site calls Supabase directly and the DataModule method would duplicate): Keep Supabase call in call site, but after success replace the direct mutation `labels = labels.filter(...)` with `DataModule.setLabels(DataModule.getLabels().filter(...))` — this keeps DataModule._labels in sync without duplicating the DB call.

Use **Option B throughout** — it is safer for a call site refactor (no behavioral change, just state sync). The call sites already handle errors, toasts, and localStorage writes. The only change is syncing DataModule's internal state so validateLabelExists() works and data-changed events fire.

However, for Option B to fire events, use `DataModule.setLabels()` only for labels mutations (it does NOT emit). For `allTxData` mutations, similarly use `DataModule.setTransactions()` (also does NOT emit). Since events aren't needed for these specific call sites (Realtime events already handle re-renders), this is acceptable. The CRITICAL path is that `DataModule._labels` is populated — which Task 1 already ensures via `DataModule.loadLabels()`. Task 2 just keeps it in sync.

**Mutation 1 — handleExpenseChange INSERT (line ~3043):**
- Old: `allTxData.unshift(newRow);`
- New: `allTxData.unshift(newRow); DataModule.setTransactions(allTxData);`

**Mutation 2 — handleExpenseChange UPDATE (line ~3047):**
- Old: `if (idx !== -1) allTxData[idx] = newRow;`
- New: `if (idx !== -1) { allTxData[idx] = newRow; DataModule.setTransactions(allTxData); }`

**Mutation 3 — handleExpenseChange DELETE (line ~3051):**
- Old: `allTxData = allTxData.filter(tx => tx.id !== oldRow.id);`
- New: `allTxData = allTxData.filter(tx => tx.id !== oldRow.id); DataModule.setTransactions(allTxData);`

**Mutation 4 — handleLabelChange INSERT (line ~3067):**
- Old: `labels.unshift(newRow);`
- New: `labels.unshift(newRow); DataModule.setLabels(labels);`

**Mutation 5 — handleLabelChange UPDATE (line ~3071):**
- Old: `if (idx !== -1) labels[idx] = newRow;`
- New: `if (idx !== -1) { labels[idx] = newRow; DataModule.setLabels(labels); }`

**Mutation 6 — handleLabelChange DELETE (line ~3074):**
- Old: `labels = labels.filter(l => l.id !== oldRow.id);`
- New: `labels = labels.filter(l => l.id !== oldRow.id); DataModule.setLabels(labels);`

**Mutation 7 — deleteLabel function (line ~3252):**
- Old: `labels = labels.filter(l => l.id !== lbl.id);`
- New: `labels = labels.filter(l => l.id !== lbl.id); DataModule.setLabels(labels);`

**Mutation 8 — renderManageLabels delete handler (line ~3316):**
- Old: `labels = labels.filter(l => l.id !== lbl.id);`
- New: `labels = labels.filter(l => l.id !== lbl.id); DataModule.setLabels(labels);`

**Mutation 9 — standalone addLabel function (line ~3344):**
- Old: `labels.unshift(data);`
- New: `labels.unshift(data); DataModule.setLabels(labels);`

**Mutation 10 — btn-ml-add handler (line ~3921):**
- Old: `labels.unshift(data);`
- New: `labels.unshift(data); DataModule.setLabels(labels);`

**Mutation 11 — deleteExpense (line ~3558):**
- Old: `allTxData = allTxData.filter(t => t.id !== id);`
- New: `allTxData = allTxData.filter(t => t.id !== id); DataModule.setTransactions(allTxData);`

**Mutation 12 — saveEdit (line ~3629):**
- Old: `if (idx >= 0) { allTxData[idx] = {...allTxData[idx], amount:amt, label_id:labelId, label_name:labelName}; }`
- New: `if (idx >= 0) { allTxData[idx] = {...allTxData[idx], amount:amt, label_id:labelId, label_name:labelName}; DataModule.setTransactions(allTxData); }`

Each change is a one-line addition (`DataModule.setLabels(labels)` or `DataModule.setTransactions(allTxData)`) directly after the existing mutation line. Do not change any error handling, rendering calls, toast messages, or localStorage writes — those stay exactly as they are.
  </action>
  <verify>
    <automated>grep -n "DataModule.setLabels\|DataModule.setTransactions" "C:/Users/achum/App Creation/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DataModule.setLabels(labels)" index.html` returns 5 (mutations 4, 5, 6, 7/8, 9 — handleLabelChange×3 + deleteLabel + renderManageLabels delete handler + addLabel) — actual count is 6 (6 labels mutation sites)
    - `grep -c "DataModule.setTransactions(allTxData)" index.html` returns 5 (mutations 1, 2, 3, 11, 12 — handleExpenseChange×3 + deleteExpense + saveEdit) — actual count is 5
    - `grep -n "DataModule.setLabels\|DataModule.setTransactions" index.html` shows results inside handleExpenseChange, handleLabelChange, deleteLabel, renderManageLabels, addLabel, btn-ml-add handler, deleteExpense, and saveEdit
    - `grep -c "DataModule.validateLabelExists" index.html` returns 1 (unchanged — still at saveEdit line ~3603)
    - The app loads without JavaScript errors (verify by checking index.html is syntactically valid: `node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); const match=html.match(/<script[^>]*>([\s\S]*?)<\/script>/g); console.log('Script tags:', match ? match.length : 0);"`)
  </acceptance_criteria>
  <done>
    - All 11+ direct allTxData/labels mutations now call DataModule.setLabels() or DataModule.setTransactions() to sync DataModule's internal state
    - DataModule._labels stays in sync with global labels across all mutation paths
    - DataModule._allTxData stays in sync with global allTxData across all mutation paths
    - validateLabelExists() now correctly checks a populated _labels array — returns true for existing labels, false for deleted ones
    - QUAL-01 fully satisfied: DataModule is the source of truth for both reads (via getters) and writes (via setters called after every mutation)
    - QUAL-03 fully satisfied: validateLabelExists() in saveEdit() is functional end-to-end
    - ChartModule data-changed listener will fire correctly when DataModule.setTransactions triggers — not from these setters (they don't emit), but DataModule is now populated so ChartModule.invalidateCache events from DataModule.loadHistory do fire
  </done>
</task>

</tasks>

<verification>
**Phase-level verification after gap closure:**

1. **QUAL-01 satisfied:**
   - `grep -n "_doLoadLabels" index.html` → no results (function removed)
   - `grep -n "DataModule.loadLabels()" index.html` → at least 1 result
   - `grep -n "DataModule.loadHistory()" index.html` → at least 1 result in _doLoadHistory
   - `grep -n "DataModule.setLabels\|DataModule.setTransactions" index.html` → results in all 11 mutation sites

2. **QUAL-03 satisfied:**
   - `grep -n "validateLabelExists" index.html` → result at saveEdit (~line 3603)
   - `grep -n "DataModule.loadLabels()" index.html` → confirms _labels will be populated at runtime
   - `grep -n "_doLoadLabels" index.html` → no results (the old standalone function that populated `labels` but not `DataModule._labels` is gone)

3. **No regressions:**
   - `grep -n "renderFilterChips\|renderHistory\|renderChips\|renderManageLabels" index.html` → still present (rendering code unchanged)
   - All global `allTxData` and `labels` reads in rendering functions still work (globals kept in sync)
</verification>

<success_criteria>
- DataModule._labels populated at runtime: `DataModule.loadLabels()` is called in the load chain, `_labels` is set inside DataModule
- DataModule._allTxData populated at runtime: `DataModule.loadHistory()` is called when navigating to history, `_allTxData` is set inside DataModule
- Standalone `_doLoadLabels()` function no longer exists in index.html
- All label mutations (deleteLabel, handleLabelChange, addLabel, btn-ml-add, renderManageLabels delete) call `DataModule.setLabels(labels)` after modifying the global
- All transaction mutations (handleExpenseChange, deleteExpense, saveEdit) call `DataModule.setTransactions(allTxData)` after modifying the global
- `validateLabelExists(editLabel.id)` in saveEdit returns `false` for deleted labels and `true` for existing labels
- App runs without JS errors: `node -e "..."` syntax check passes
- ChartModule cache invalidation fires on history load: DataModule.loadHistory() emits 'data-changed', ChartModule listener (line 1564) calls invalidateCache()
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality-maintainability/05-04-SUMMARY.md`
</output>
